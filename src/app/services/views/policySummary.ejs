<h1 class="govuk-heading-l govuk-!-margin-top-9 text-break">
    <span class="govuk-caption-l"><%= locals.service.name %></span>
    <%= locals.policy.name %>
</h1>
<dl class="inline">
    <dt class="govuk-label">Status:</dt>
    <dd class="govuk-label"><%= locals.policy.status && locals.policy.status.id === 1 ? 'Active' : 'Inactive' %></dd>
</dl>
